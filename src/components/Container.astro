---
import Icon from './Icon.astro';

interface Props {
  title: string;
  icon?: string;
  class?: string;
}

const { title, icon, class: className } = Astro.props;
---

<div class:list={['rounded-2xl border border-gray-200 bg-white shadow-sm overflow-hidden', className]}>
  <!-- Header -->
  <div class="flex items-center gap-3 px-6 py-4 border-b border-gray-100">
    {icon && (
      <span class="flex-shrink-0 text-brand">
        <Icon name={icon} class="w-5 h-5" />
      </span>
    )}
    <h3 class="text-base font-semibold text-gray-900">{title}</h3>
  </div>

  <!-- Content -->
  <div class="px-6 py-5">
    <slot />
  </div>

  <!-- Footer: flex, align-items stretch, justify-content end, gap 16px -->
  <div class="px-6 py-4 border-t border-gray-100 flex items-stretch justify-end gap-4">
    <slot name="footer" />
  </div>
</div>
